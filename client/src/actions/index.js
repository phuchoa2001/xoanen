import * as types from "../contants/Actiontypes"
export const hidemenumobile = () => {
     return {
         type: types.HIDE_MENUMOBILE
     }
}
export const togglemenumobile = () => {
    return {
        type: types.TOGGLE_MENUMOBILE
    }
}